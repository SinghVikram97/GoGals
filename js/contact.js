window.onload = init;

function init() {
    var typed = new Typed('.typed', {
        stringsElement: '#typed-strings',
        typeSpeed: 100,
        backDelay: 1000,
        backSpeed: 100,
        loop: true
    });
}